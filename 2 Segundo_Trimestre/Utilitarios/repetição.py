'''

O for _ in é uma estrutura de repetição em Python usada para iterar sobre elementos de uma sequência (como uma lista, tupla ou string). 
O _ é uma convenção para indicar que a variável não será utilizada dentro do loop.

Exemplo:
'''
for _ in range(5):
    print("Repetição")

'''

O `range` é uma função em Python que gera uma sequência de números, normalmente usada em loops `for`. 
Ela cria uma sequência que começa em 0 (por padrão) e vai até um número específico (não incluído).

Exemplos:

- `range(5)` gera `0, 1, 2, 3, 4`.
- `range(2, 7)` gera `2, 3, 4, 5, 6`.
- `range(1, 10, 2)` gera `1, 3, 5, 7, 9` (com passo 2).

'''